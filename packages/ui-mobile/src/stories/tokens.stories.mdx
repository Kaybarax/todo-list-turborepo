import { Meta, ColorPalette, ColorItem, Typeset } from '@storybook/blocks';
import { lightColors, darkColors } from '@/tokens/colors.js';
import { spacing } from '@/tokens/spacing.js';
import { fontSizes, fontWeights, lineHeights } from '@/tokens/typography.js';
import { borders } from '@/tokens/borders.js';
import { shadows } from '@/tokens/shadows.js';

<Meta title="Design System/Tokens" />

# Design Tokens

Design tokens are the visual design atoms of the design system — specifically, they are named entities that store visual design attributes. We use them in place of hard-coded values (such as hex values for color or pixel values for spacing) in order to maintain a scalable and consistent visual system.

## Colors

### Light Theme Colors

<ColorPalette>
  <ColorItem
    title="Primary"
    subtitle="Brand colors"
    colors={{
      50: lightColors.primary[50],
      100: lightColors.primary[100],
      200: lightColors.primary[200],
      300: lightColors.primary[300],
      400: lightColors.primary[400],
      500: lightColors.primary[500],
      600: lightColors.primary[600],
      700: lightColors.primary[700],
      800: lightColors.primary[800],
      900: lightColors.primary[900],
    }}
  />
  <ColorItem
    title="Secondary"
    subtitle="Supporting colors"
    colors={{
      50: lightColors.secondary[50],
      100: lightColors.secondary[100],
      200: lightColors.secondary[200],
      300: lightColors.secondary[300],
      400: lightColors.secondary[400],
      500: lightColors.secondary[500],
      600: lightColors.secondary[600],
      700: lightColors.secondary[700],
      800: lightColors.secondary[800],
      900: lightColors.secondary[900],
    }}
  />
  <ColorItem
    title="Success"
    subtitle="Success states"
    colors={{
      50: lightColors.success[50],
      100: lightColors.success[100],
      200: lightColors.success[200],
      300: lightColors.success[300],
      400: lightColors.success[400],
      500: lightColors.success[500],
      600: lightColors.success[600],
      700: lightColors.success[700],
      800: lightColors.success[800],
      900: lightColors.success[900],
    }}
  />
  <ColorItem
    title="Warning"
    subtitle="Warning states"
    colors={{
      50: lightColors.warning[50],
      100: lightColors.warning[100],
      200: lightColors.warning[200],
      300: lightColors.warning[300],
      400: lightColors.warning[400],
      500: lightColors.warning[500],
      600: lightColors.warning[600],
      700: lightColors.warning[700],
      800: lightColors.warning[800],
      900: lightColors.warning[900],
    }}
  />
  <ColorItem
    title="Error"
    subtitle="Error states"
    colors={{
      50: lightColors.error[50],
      100: lightColors.error[100],
      200: lightColors.error[200],
      300: lightColors.error[300],
      400: lightColors.error[400],
      500: lightColors.error[500],
      600: lightColors.error[600],
      700: lightColors.error[700],
      800: lightColors.error[800],
      900: lightColors.error[900],
    }}
  />
</ColorPalette>

### Semantic Colors

<ColorPalette>
  <ColorItem
    title="Background"
    colors={{
      primary: lightColors.background.primary,
      secondary: lightColors.background.secondary,
      tertiary: lightColors.background.tertiary,
    }}
  />
  <ColorItem
    title="Text"
    colors={{
      primary: lightColors.text.primary,
      secondary: lightColors.text.secondary,
      tertiary: lightColors.text.tertiary,
      inverse: lightColors.text.inverse,
    }}
  />
  <ColorItem
    title="Border"
    colors={{
      primary: lightColors.border.primary,
      secondary: lightColors.border.secondary,
      focus: lightColors.border.focus,
    }}
  />
</ColorPalette>

## Typography

### Font Sizes

<Typeset
  fontSizes={[
    fontSizes.xs,
    fontSizes.sm,
    fontSizes.base,
    fontSizes.lg,
    fontSizes.xl,
    fontSizes['2xl'],
    fontSizes['3xl'],
    fontSizes['4xl'],
  ]}
  fontWeight={fontWeights.normal}
  sampleText="The quick brown fox jumps over the lazy dog"
/>

### Font Weights

<div style={{ display: 'flex', flexDirection: 'column', gap: '16px' }}>
  <div style={{ fontWeight: fontWeights.light, fontSize: fontSizes.lg }}>
    Light ({fontWeights.light}) - The quick brown fox jumps over the lazy dog
  </div>
  <div style={{ fontWeight: fontWeights.normal, fontSize: fontSizes.lg }}>
    Normal ({fontWeights.normal}) - The quick brown fox jumps over the lazy dog
  </div>
  <div style={{ fontWeight: fontWeights.medium, fontSize: fontSizes.lg }}>
    Medium ({fontWeights.medium}) - The quick brown fox jumps over the lazy dog
  </div>
  <div style={{ fontWeight: fontWeights.semibold, fontSize: fontSizes.lg }}>
    Semibold ({fontWeights.semibold}) - The quick brown fox jumps over the lazy dog
  </div>
  <div style={{ fontWeight: fontWeights.bold, fontSize: fontSizes.lg }}>
    Bold ({fontWeights.bold}) - The quick brown fox jumps over the lazy dog
  </div>
</div>

## Spacing

The spacing scale is based on a 4px grid system for consistent rhythm and alignment.

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: '16px', marginTop: '24px' }}>
  {Object.entries(spacing).map(([key, value]) => (
    <div key={key} style={{ display: 'flex', alignItems: 'center', gap: '12px' }}>
      <div
        style={{
          width: value,
          height: '24px',
          backgroundColor: lightColors.primary[500],
          borderRadius: '2px',
        }}
      />
      <span style={{ fontFamily: 'monospace', fontSize: '14px' }}>
        {key}: {value}px
      </span>
    </div>
  ))}
</div>

## Border Radius

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(150px, 1fr))', gap: '16px', marginTop: '24px' }}>
  {Object.entries(borders.radius).map(([key, value]) => (
    <div key={key} style={{ textAlign: 'center' }}>
      <div
        style={{
          width: '80px',
          height: '80px',
          backgroundColor: lightColors.primary[500],
          borderRadius: value,
          margin: '0 auto 8px',
        }}
      />
      <div style={{ fontFamily: 'monospace', fontSize: '14px' }}>
        {key}: {value}px
      </div>
    </div>
  ))}
</div>

## Shadows

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))', gap: '24px', marginTop: '24px' }}>
  {Object.entries(shadows).map(([key, value]) => (
    <div key={key} style={{ textAlign: 'center' }}>
      <div
        style={{
          width: '120px',
          height: '80px',
          backgroundColor: lightColors.background.primary,
          borderRadius: borders.radius.md,
          margin: '0 auto 12px',
          boxShadow: value,
        }}
      />
      <div style={{ fontFamily: 'monospace', fontSize: '14px', marginBottom: '4px' }}>
        {key}
      </div>
      <div style={{ fontSize: '12px', color: lightColors.text.secondary }}>
        {value}
      </div>
    </div>
  ))}
</div>

## Usage Guidelines

### Color Usage
- Use primary colors for main actions and brand elements
- Use secondary colors for supporting elements
- Use semantic colors (success, warning, error) for status indicators
- Ensure proper contrast ratios for accessibility (4.5:1 for normal text, 3:1 for large text)

### Typography Usage
- Use consistent font sizes from the scale
- Maintain proper line height for readability
- Use font weights to establish hierarchy

### Spacing Usage
- Use the 4px grid system for consistent spacing
- Apply spacing tokens instead of arbitrary values
- Maintain consistent margins and padding

### Border Radius Usage
- Use consistent border radius values
- Match border radius to component size and context
- Maintain visual consistency across similar components
