import { Meta } from '@storybook/blocks';

<Meta title="Setup/Intro" />

# ui-mobile Storybook

This is a minimal docs-only page used to boot Storybook without loading any component stories while we debug and fix them one by one.

Once a story is verified, we will re-enable it in the configuration.

